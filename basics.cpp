#include <iostream> //header file library that allows us to work with input and output objects
using namespace std; //standard library that lets us use names for objects and variables from the standard library
//whitespace for readable code
int main(){
    cout << "Hello World!";
    return 0;
}
/*
main == a function 
cout == console output/ object used together with the insertion operator to output text 
<< == insertion operator 
return 0 == closes the function
*/